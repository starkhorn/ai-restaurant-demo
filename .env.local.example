# Next.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here

# Vercel Postgres Database Configuration
# Required: Main database connection URL
POSTGRES_URL="postgres://username:password@hostname:port/database?sslmode=require"

# Required for migrations and schema operations (non-pooling connection)
POSTGRES_URL_NON_POOLING="postgres://username:password@hostname:port/database?sslmode=require"

# Optional: Individual connection parameters (alternative to POSTGRES_URL)
# POSTGRES_USER=your_username
# POSTGRES_HOST=your_hostname
# POSTGRES_PASSWORD=your_password
# POSTGRES_DATABASE=your_database_name

# Note: When using Vercel Postgres, these environment variables are automatically
# provided in your Vercel deployment. For local development, you'll need to
# create a Vercel Postgres database and copy the connection strings from your
# Vercel dashboard.